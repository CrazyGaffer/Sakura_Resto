import './UpcomingEvents.css'; // Create this CSS file for custom styles

const UpcomingEvents = () => {
    return (
        <div className="upcoming-events">
            <h2>Upcoming Events</h2>
            <p>Discover our exciting upcoming events and mark your calendars for a unique experience.</p>
        </div>
    );
};

export default UpcomingEvents;
